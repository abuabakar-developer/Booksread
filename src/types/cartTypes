// src/types/cartTypes.ts

export interface Book {
    id: string;
    title: string;
    quantity: number;
    price: number;
    cover_image: string;
}

export interface CartState {
    books: Book[];
}
